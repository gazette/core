# Soak runs a soak-test of Gazette, with all resources applied to the
# "soak" namespace.
bases:
  - ../../../bases/etcd
  - ../../../bases/minio
  - ../../../bases/broker
  - ../example-stream-sum
  - ../environment
  - ../integration
patchesStrategicMerge:
  - chunker-runs-forever.yaml
resources:
  - namespace.yaml

namespace: soak

commonLabels:
  app.kubernetes.io/instance: soak
